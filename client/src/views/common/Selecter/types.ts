export type PropsTypes = {
  input:any; label: string;
  meta: any;
  placeholder?: any;
  options: Array<any>;
  disabled?: boolean;
  defaultValue?: string;
};
export type ValueTypes = {
  label: string;
  value: string;
};